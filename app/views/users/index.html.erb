<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
    <%= render @users %>
    <!--
    <% @users.each do |user| %>
        <%= render user %>  ここでのuserはeach文の変数であるuserを指している
    <% end %>
    -->
</ul>

 <%= will_paginate %> 
